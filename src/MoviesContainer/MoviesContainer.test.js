import { render, screen, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom';
import MovieContainer from './MovieContainer';

describe('MovieContainer', () => {
  it('should return movieCard on page load', () => {
    // setup
    // render the movieCard, with mocked const(s) of movies and mock functions of movieClicked displaySingleMovie
    // execution
    // 
    // assertion
    // expect the const to be in the document
  })
})